apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: simple-petclinic
  name: simple-petclinic
spec:
  port:
    targetPort: 8080-tcp
  to:
    kind: Service
    name: simple-petclinic
    weight: 100