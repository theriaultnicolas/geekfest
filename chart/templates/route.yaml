apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: simple-echo
  name: simple-echo
spec:
  port:
    targetPort: tcp-endpoint
  to:
    kind: Service
    name: simple-echo
    weight: 100