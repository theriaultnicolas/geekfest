apiVersion: v1
kind: Service
metadata:
  labels:
    app: simple-echo
  name: simple-echo
spec:
  ports:
  - name: tcp-endpoint
    port: {{ .Values.port }}
    protocol: TCP
    targetPort: {{ .Values.port }}
  selector:
    app: simple-echo
  sessionAffinity: None
  type: ClusterIP